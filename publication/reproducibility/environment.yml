# Conda environment for Transition-Field Geometry paper
# Exact versions for reproducibility

name: sgp_core
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.14
  - numpy=2.2
  - scipy=1.15
  - scikit-learn=1.6
  - matplotlib=3.10
  - pandas=2.2
  - pip:
    - tabulate
